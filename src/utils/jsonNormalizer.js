// TODO (Zadanie 1): Adapter — format JSON Insly → schemat wewnętrzny
// IddForm { id, title, sections: Section[] }
// Section { id, title, order, fields: Field[] }
// Field { id, type, label, required, order, options? }
export function normalizeIdd(rawJson) {
  return rawJson
}
