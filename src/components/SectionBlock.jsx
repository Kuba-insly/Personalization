// Placeholder for Zadanie 4 — will wrap fields in a sortable drag context.
// For now renders children directly with a visual grouping wrapper.
export default function SectionBlock({ children }) {
  return <div className="section-block">{children}</div>
}
